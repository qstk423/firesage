# 复制为 .env.local 后填写。不要将真实密钥提交到 Git。
LLM_BASE_URL=https://api.deepseek.com
LLM_API_KEY=
LLM_MODEL=deepseek-v4-flash

# 可选安全（试点）
# API_KEY=your-secret
# RATE_LIMIT_PER_MIN=60
# AUDIT_DIR=  # 默认 backend/data/audit
# AUDIT_DISABLED=0
