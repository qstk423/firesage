# Docker 演示镜像依赖（不含 torch / sentence-transformers / faiss）
# 默认 FIRESAGE_LITE=1：BM25 + TF-IDF + GraphRAG + 规则重排，镜像小、启动快。
# 需要全量 bge 时：改用 requirements.txt，并去掉 compose 里的 FIRESAGE_LITE。
fastapi>=0.110,<1
uvicorn>=0.29,<1
jieba>=0.42,<1
rank-bm25>=0.2.2,<1
requests>=2.31,<3
numpy>=1.26,<3
scikit-learn>=1.3
joblib>=1.3
